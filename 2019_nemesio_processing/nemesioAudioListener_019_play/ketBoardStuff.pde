void keyPressed(){
  /*
  if(key=='0')setMode(0,0);
  if(key=='p')setMode(0,1);
  if(key=='ñ')setMode(0,2);
  if(key=='-')setMode(0,3);
  
  if(key=='1')setMode(1,0);
  if(key=='q')setMode(1,1);
  if(key=='a')setMode(1,2);
  if(key=='z')setMode(1,3);
  
  
  if(key=='2')setMode(2,0);
  if(key=='w')setMode(2,1);
  if(key=='s')setMode(2,2);
  if(key=='x')setMode(2,3);
  
  if(key=='3')setMode(3,0);
  if(key=='e')setMode(3,1);
  if(key=='d')setMode(3,2);
  if(key=='c')setMode(3,3);
  
  if(key=='4')setMode(4,0);
  if(key=='r')setMode(4,1);
  if(key=='f')setMode(4,2);
  if(key=='v')setMode(4,3);
  
  if(key=='5')setMode(5,0);
  if(key=='t')setMode(5,1);
  if(key=='g')setMode(5,2);
  if(key=='b')setMode(5,4);
  
  if(key=='6')setMode(6,0);
  
  if(key=='k')randInAll = !randInAll;
  if(key=='l')randInMe  = !randInMe;
  
  if(key=='u')wShaderV  = 4;
  if(key=='i')wShaderV  = int(random(4));*/
}
